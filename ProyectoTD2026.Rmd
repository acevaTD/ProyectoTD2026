---
title: "Proyecto Registros Tratamiento de Datos Curso 2024-2025"
author:
  - name: "Aleix Cebrián Valero"
    affiliation: a
  - name: "Carlos Albiach Melgar"
    affiliation: b
address:
  - code: a
    address: "leutnant@fh-muenster.de"
  - code: b
    address: "caralmel@alumni.uv.es"
abstract: |
  Este proyecto tiene como objetivo hallar información a partir de un archivo CSV con los registros de los usuarios de TD 2024-2025. 
  
  *Nota: Todos los miembros han participado equitativamente en el trabajo.*
geometry: margin=2cm

header-includes:
  - \newcounter{none}
  - \usepackage{booktabs}
  - \usepackage{longtable}
  - \providecommand{\pandocbounded}[1]{#1}
  - \usepackage{pdflscape}
  
output: 
  rticles::elsevier_article:
    keep_tex: true
---
\newpage

```{r include=FALSE}
# Importación del archivo .csv

library(readr)
df_users <- read_csv("data/logs_moodle_anon.csv",
                     skip = 1,
    col_names = c("Fecha","NombreUsuario","UsuarioAfectado", "ContextoEvento", 
                  "Componente", "NombreEvento", "Descripcion", "Origen", "IP"),
    show_col_types = FALSE)

# Fecha como tipo POSIXlt

df_users$Fecha <- as.POSIXlt(sub(",","",df_users$Fecha), 
                             format = "%d/%m/%y %H:%M:%S")
class(df_users$Fecha)

```

```{r include=FALSE}
# NombreUsuario, Componente y Origen definidos como factores

df_users$NombreUsuario <- as.factor(df_users$NombreUsuario)
df_users$Componente <- as.factor(df_users$Componente)
df_users$Origen <- as.factor(df_users$Origen)

```

# Componentes del fichero
Hay un total de 26 compnenetes en el fichero.


```{r tab_componentes}
type <- as.character(unique(df_users$Componente))
knitr::kable(rticles::string_to_table(type, 3), 
             col.names = NULL, format = "latex", booktab = TRUE, 
             caption = "Componentes del fichero.")
```

Como se puede observar en la Tabla \ref{tab:tab_componentes}, hay 26 componentes.

# Usuarios más activos
Vamos a ver que usuario ha sido el que más veces se ha registrado.

```{r fig1, fig.align='center', out.width="100%", echo=FALSE, fig.cap="A figure added with a code chunk.\\label{fig:fig1}, echo = FALSE, message=FALSE, warning=FALSE, results='hide'"}
library(ggplot2)
table(df_users$NombreUsuario)[1]
ggplot(df_users, aes(x = NombreUsuario)) + geom_bar() +
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust = 1))
```

Como podemos ver en la Figura \ref{fig:fig1}, hay un tipo to rayao.

# Usuarios más activos 2

Vamos a ver que usuario ha sido el que más veces se ha registrado.
I\par

\begin{landscape}


```
\section{Usuarios más activos}
```

```{r fig3, fig.align='left', out.width="100%", fig.cap = "\\label{fig2}Bar plot horizontal!!.", echo = FALSE, message=FALSE, warning=FALSE, results='hide'}
library(gridExtra)
library(grid)
library(ggplot2)
table(df_users$NombreUsuario)[1]
ggplot(df_users, aes(x = NombreUsuario)) + geom_bar() +
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust = 1), plot.background = element_rect(color = "black"))

```

\end{landscape}


Como podemos ver en la Figura \ref{fig:fig3}, hay un tipo to rayao.

# Mes con más registros

Los meses en los que más registros han habido

```{r fig4, fig.align='center', out.width="100%", fig.cap = "\\label{fig2}Bar plot horizontal!!.", echo = FALSE, message=FALSE, warning=FALSE, results='hide'}
library(dplyr)
library(kableExtra)
#library(lubridate)

meses_frec <- df_users %>% group_by(df_users$Fecha$mon + 1) %>% count()
names(meses_frec) <- c("Mes", "Total")
kable(meses_frec)
meses_frec$Mes <- as.factor(meses_frec$Mes)

ggplot(meses_frec, aes(x = Mes, y = Total)) +
  scale_x_discrete(labels = c("Enero", "Febrero", "Marzo", "Abril", "Mayo", 
                              "Junio", "Julio", "Agosto", "Septiembre", 
                              "Octubre", "Noviembre", "Diciembre")) +
  geom_segment(aes(x = Mes, xend = Mes,
                   y = 0, yend = Total)) +
  geom_point(size = 6, pch = 21, bg = 4, col = 1) +
  geom_text(aes(label = Total), vjust = -1, color = "black", size = 3.5) +
  coord_flip() + theme_minimal()

#nrow(df_users %>%
  #filter(month(Fecha) == 5))

```

# Contexto con más registros

Los contextos más visitados son:

```{r}
library(dplyr)
df_users %>% group_by(ContextoEvento) %>% count() %>% filter(n<30000)

ggplot(df_users %>% group_by(ContextoEvento) %>% count() %>% filter(n<30000 & n > 500), aes(x = ContextoEvento, y = n)) + geom_col() +
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust = 1), plot.background = element_rect(color = "black"))
```

```{r}
# Todos los componentes
library(stringr)

#df_teacher <- df_users %>% filter(NombreUsuario == "U0072")

ggplot(df_users, aes(x = Componente)) + geom_bar()+
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust = 1), plot.background = element_rect(color = "black"))

## Todos los cuestionarios
ggplot(df_users %>% filter(Componente == "Cuestionario"), aes(x = ContextoEvento)) + geom_bar()+
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust = 1), plot.background = element_rect(color = "black"))
nrow(df_users %>% filter(Componente == "Cuestionario") %>% distinct(ContextoEvento)) # Cantidad de cuestionarios

## Todas las tareas
ggplot(df_users %>% filter(Componente == "Tarea"), aes(x = ContextoEvento)) + geom_bar()+
  theme(axis.text.x = element_text(angle = 90, vjust = 0.5, hjust = 1), plot.background = element_rect(color = "black"))
nrow(df_users %>% filter(Componente == "Tarea") %>% distinct(ContextoEvento)) # Cantidad de tareas
```

# Usuarios sin retroalimentación

```{r}
#table((df_users %>% filter(!is.na(UsuarioAfectado))) == (df_users %>% filter(!is.na(UsuarioAfectado))))

df_users_usuario_afectado <- df_users %>% filter(!is.na(UsuarioAfectado) & UsuarioAfectado != NombreUsuario)

unique(df_users_usuario_afectado$NombreEvento)

unique((df_users_usuario_afectado %>% filter(NombreEvento == "Usuario matriculado en el curso"))$UsuarioAfectado)

length(unique(df_users$UsuarioAfectado))
```

Parece que el usuario U0071 no ha hecho nada en todo el curso, y U0072 sha sido 
desmatriculado.


# ¿Cuál es el proveedor de servicios más común?

```{r}
# Agrupamos por la columna IP y contamos las frecuencias
proveedor_counts <- as.data.frame(table(df_users$IP))
colnames(proveedor_counts) <- c("IP", "Frecuencia")
proveedor_counts <- proveedor_counts[order(-proveedor_counts$Frecuencia), ]

# Mostramos las 10 primeras
head(proveedor_counts, 10)

# Gráfico para obtener las conclusiones visualmente
library(ggplot2)
ggplot(head(proveedor_counts, 10), aes(x = reorder(IP, Frecuencia), y = Frecuencia)) + geom_col(fill = "blue") + coord_flip() + labs(title = "Top 10 Direcciones IP (Proveedores)", x = "Dirección IP", y = "Número de interacciones") + theme_minimal()
```

El proveedor de servicios más común se identifica mediante el análisis de las direcciones IP. Según la información que he encontrado, la IP más frecuente que observamos en la solución del código, suele pertenecer a la red de la Universitat de València (RedIRIS). Lo cual es lógico ya que los usuarios acceden principalmente desde el campus o mediante la VPN universitaria.


# ¿Hay algún usuario afectado que no sea él mismo?

```{r}
# Filtramos los registros donde UsuarioAfectado no es NA y es distinto al NombreUsuario
usuarios_afectados_distintos <- df_users[(!is.na(df_users$UsuarioAfectado)) & (df_users$UsuarioAfectado != "NA") & (df_users$UsuarioAfectado != df_users$NombreUsuario), ]

# Vemos si hay resultados
nrow(usuarios_afectados_distintos)

# Tipo de eventos
unique(usuarios_afectados_distintos$NombreEvento)
```

Podemos ver que existen numerosos registros donde el usuario afectado es distinto al autor de la acción (15682). Esto sucede en eventos como los que se han impreso por pantalla, cuando un usuario visualiza el perfil de otro, cuando se realizan matriculaciones en grupos,... Por ejemplo podemos observar en el dataframe, registros como el del usuario U0072 que interactúa con el perfil o datos de otros usuarios como U0055.


# ¿Qué meses hay más tareas?

```{r}
# Filtramos por el componente "Tarea"
tareas <- df_users[df_users$Componente == "Tarea", ]

# Extraemos el mes, Fecha$mon devuelve los meses del 0 al 11, así que sumamos 1
meses_tareas <- table(tareas$Fecha$mon + 1)

# Creamos un dataframe para el gráfico
df_meses_tareas <- as.data.frame(meses_tareas)
colnames(df_meses_tareas) <- c("Mes", "Cantidad")

# Definimos los nombres de los meses
nombres_meses <- c("Enero", "Febrero", "Marzo", "Abril", "Mayo", "Junio", "Julio", "Agosto", "Septiembre", "Octubre", "Noviembre", "Diciembre")

# Gráfico para obtener las conclusiones visualmente
library(ggplot2)
ggplot(df_meses_tareas, aes(x = Mes, y = Cantidad)) + geom_bar(stat = "identity", fill = "green") + scale_x_discrete(labels = nombres_meses[as.numeric(levels(df_meses_tareas$Mes))]) + labs(title = "Actividad de Tareas por Mes", x = "Mes", y = "Total de registros") + theme_minimal()
```

Gracias al gráfico anterior vemos que el pico máxima de actividad es en el mes de febrero, con mucha diferencia. Esto indica que es el mes donde se concentran la mayoría de las entregas finales, correcciones, cierres de actividades evaluables del primer semestre o inicio del segundo,...
Los meses de abril, marzo y mayo presentan niveles de actividad moderados pero constantes, lo que sugiere un flujo de trabajo continuo durante el segundo cuatrimestre.
